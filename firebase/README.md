## Firebase folder

This folder contains configuration that is safe to commit:

- `firestore.rules`
- `storage.rules`

Cloud Functions + emulator configs will be added in the next todo (`functions-core`).

